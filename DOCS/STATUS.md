# STATUS

## Current Phase
- name: 7. APP SHELL + INFORMATION ARCHITECTURE
- status: in_progress
- owner: cursor
- started_at: 2026-04-24
- updated_at: 2026-04-25

## Current Task
- id: P7-9-3-outcomes-index-query-project-id
- title: На **`/outcomes`** — поддержать **`?project_id=<uuid>`** (override к slug **`fabrika-v1-sample`** по умолчанию): валидация UUID, честная ошибка при **400** от API; **без** CRUD/detail; toolchain + **`STATUS`**
- status: planned
- linked_phase: 7

## Phase 6 — `MASTER` §6 vs фактический baseline (**P6-master-section6-dod-reconcile**, 2026-04-24)

**Узкая трактовка канона:** действует блок **Canonical v1 alignment** в **`MASTER` §6** + **`DATA_MODEL_V1.md` / `ARCHITECTURE_V1.md`** — **пять** таблиц (`project`, `outcome`, `assumption`, `acceptance_criterion`, `evidence_item`). Буллеты **`MASTER` §6.1–§6.2** (множественные имена, лишние сущности) **не** считаются требованиями к коду.

| Ожидание §6 (узко) | Факт в репо | Smoke / evidence |
|--------------------|-------------|------------------|
| Минимальная схема пяти сущностей + индексы / timestamps | `db/schema.ts` + `db/migrations/0000_*.sql` | Toolchain build ранее; миграция применяется средой с **`DATABASE_URL`** |
| §6.3 seed одного sample | `scripts/seed-sample-project.mjs` (идемпотентный slug `fabrika-v1-sample`) | **`node --env-file=.env.local scripts/seed-sample-project.mjs`** — **exit 0** (**STATUS**, **P6-evidence-item-post-read-smoke**) |
| «CRUD» для пяти сущностей (узко: **C+R** + **U** + **D**) | **`GET` + `POST`**; **`PATCH`** (одно поле) для **всех пяти**; **`DELETE`** **`/api/.../[id]`** для **всех пяти** (**`204`**, **`400`**, **`404`**, **`409`** при FK / RESTRICT как в БД) | **Авто-smoke** **`scripts/smoke-api-five-entities.mjs`**: seed → GET/POST → PATCH → DELETE (одноразовые строки) + проверка **`409`** на sample **`project`** — **exit 0** на **`http://127.0.0.1:4020`** (**2026-04-24**, **`run_id=1777065949717`**, **`P6-five-entities-delete-minimal`**) |
| Literal **Update / Delete** (все пять сущностей) | **Минимальный `PATCH`** + **`DELETE`** для **всех пяти** | HTTP-слой **CRUD** для пяти таблиц канона — **verified** (см. smoke); **закрытие фазы 6** как процесса (vs старт **§7**) — см. **`Current Task` / `MASTER` footnote** |

**Итог честности:** подфаза «минимальная персистенция + **CRUD по HTTP** для пяти сущностей **`DATA_MODEL_V1`**» — **verified**; **фаза 6** в смысле *только* data/API — **implemented**; **переход к §7 (shell/UI)** — **planned** (**`Current Task`**); **release-ready продукт** — **нет**.

## Closed phases (recent)

- **6. DATA MODEL V1 (MINIMUM)** — `done`, closed_at **2026-04-24**, closing_task **P6-five-entities-delete-minimal** *(пять таблиц **`DATA_MODEL_V1`**: миграция + seed + HTTP **GET/POST/PATCH/DELETE** + **`smoke-api-five-entities`**; не полный ERD из §6.1–6.2)*  
- **5. CURSOR GOVERNANCE LAYER** — `done`, closed_at **2026-04-23**, closing_task **P5-5.3-templates** *(§5.1–§5.3: governance + templates; Definition of done §5 в **`MASTER`**)*  
- **4. DOCS AS SOURCE OF TRUTH** — `done`, closed_at **2026-04-23**, closing_task **P4-4.3-release-criteria** *(§4.3 canonical docs incl. **`RELEASE_CRITERIA_V1`**; см. **`MASTER`** Definition of done §4)*  
- **3. Repository Bootstrap** — `done`, closed_at **2026-04-23**, closing_task **RB-3.phase-close-sync** *(MASTER §3.1–§3.3 согласованы с репозиторием)*

## Goal of This Phase
- **Фаза 7:** минимальный **app shell** и информационная архитектура по **`MASTER` §7** + **`UI_V1.md`** — **без** симуляции product-ready и **без** расширения за пределы v1 scope.

## Done Criteria (фаза 4 — закрыта)
- [x] **`DOCS/INDEX.md`** — канонический указатель (**P4-4.1-index**); синхронизирован с **`V1_SCOPE.md`**
- [x] **`DOCS/V1_SCOPE.md`**
- [x] **`MASTER_TODO_CURSOR.md` §4.1**
- [x] **§4.2** целиком: RFC, ERD, event, UI, UX extracts (**P4-4.2-***)
- [x] **§4.3 —** **`DOCS/ARCHITECTURE_V1.md`** + строка в MASTER (**P4-4.3-architecture**)
- [x] **§4.3** — `DATA_MODEL_V1` (**P4-4.3-data-model**)
- [x] **§4.3** — `EVENTS_V1` (**P4-4.3-events**)
- [x] **§4.3** — `UI_V1` (**P4-4.3-ui**)
- [x] **§4.3** — `RELEASE_CRITERIA_V1` (**P4-4.3-release-criteria**)

## Done Criteria (фаза 6 — закрыта)
- [x] **`P6-read-master-from-section-6`** — сверка **`MASTER` §6+** с **`DATA_MODEL_V1.md`** и `db/schema.ts`; примечание в **`MASTER` §6**; честный вход в фазу (**2026-04-23**)
- [x] **Пять таблиц** в Drizzle + файл миграции в репо (**`db/migrations/0000_*.sql`**); применение локально зависит от **`DATABASE_URL`** (не gate для **`next build`**)
- [x] **`P6-evidence-item-post-minimal`** — минимальный **`POST /api/evidence-items`** + честные ошибки; toolchain **`typecheck` / `lint` / `test` / `build`** — **2026-04-24**
- [x] **Ручной** smoke POST→GET read-back (**`P6-evidence-item-post-read-smoke`**, **2026-04-24**) — evidence ниже
- [x] **`P6-master-section6-dod-reconcile` (2026-04-24)** — сводка **§6 vs baseline** в секции **«Phase 6 — `MASTER` §6 vs фактический baseline»** (выше в этом файле); **фаза 6** остаётся **`in_progress`** (литеральный **U/D** из формулировки «CRUD» в **`MASTER` §6** не выполнен)
- [x] **Literal `MASTER` §6 DoD** (узко: HTTP **CRUD** для пяти канонических сущностей — **GET/POST/PATCH(one field)/DELETE**) — **verified** (**`P6-five-entities-delete-minimal`**, **2026-04-24**, см. **What Was Completed**); **широкий** §6.1–6.2 ERD — по-прежнему **out of scope**
- [x] **Симметричный automated smoke** для всех пяти API — **`P6-scripted-smoke-five-apis`** (**2026-04-24**)
- [x] **`README-local-smoke-five-apis`** (**2026-04-24**) — в **`README.md`** задокументирован локальный запуск **`npm run smoke:api-five`** (предпосылки, опциональный base URL, scope проверки)
- [x] **`MASTER-§6-dod-footnote-C+R-only`** (**2026-04-24**) — в **`DOCS/MASTER_TODO_CURSOR.md` §6** под Definition of done добавлен blockquote **Footnote — verified baseline vs full CRUD** (verified **C+R** vs полный **CRUD**; **`in_progress`** до **U/D** или **DEC** о сужении DoD); **`DECISIONS`** не менялись (только clarification)
- [x] **`P6-outcome-patch-minimal`** (**2026-04-24**) — **`PATCH /api/outcomes/[id]`** (только **`title`** в теле); **`app/api/outcomes/[id]/route.ts`**; **`mapOutcomeRow`** / **`UUID_RE`** экспортированы из **`app/api/outcomes/route.ts`**; smoke расширен в **`scripts/smoke-api-five-entities.mjs`**; **`README.md`** (секция smoke) синхронизирован с фактом **PATCH**; toolchain + smoke — см. **What Was Completed**
- [x] **`P6-project-patch-minimal`** (**2026-04-24**) — **`PATCH /api/projects/[id]`** (только **`name`**); **`app/api/projects/[id]/route.ts`**; **`mapProjectRow`** / **`UUID_RE`** в **`app/api/projects/route.ts`**; smoke — блок **PATCH project**; **`README`**; toolchain + **`http://127.0.0.1:3999`** — см. **What Was Completed**
- [x] **`P6-assumption-patch-minimal`** (**2026-04-24**) — **`PATCH /api/assumptions/[id]`** (только **`description`**); **`app/api/assumptions/[id]/route.ts`**; **`mapAssumptionRow`** / **`UUID_RE`** в **`app/api/assumptions/route.ts`**; smoke — блок **PATCH assumption**; **`README`**; toolchain + live smoke — см. **What Was Completed**
- [x] **`P6-acceptance-criterion-patch-minimal`** (**2026-04-24**) — **`PATCH /api/acceptance-criteria/[id]`** (только **`description`**); **`app/api/acceptance-criteria/[id]/route.ts`**; **`mapAcceptanceCriterionRow`** / **`UUID_RE`** в **`app/api/acceptance-criteria/route.ts`**; smoke — блок **PATCH AC**; **`README`**; toolchain + live smoke — см. **What Was Completed**
- [x] **`P6-evidence-item-patch-minimal`** (**2026-04-24**) — **`PATCH /api/evidence-items/[id]`** (только **`title`**); **`app/api/evidence-items/[id]/route.ts`**; **`UUID_RE`** / **`mapEvidenceRow`** экспортированы из **`app/api/evidence-items/route.ts`**; smoke — блок **PATCH evidence_item**; **`README`**; toolchain + live smoke — см. **What Was Completed**
- [x] **`P6-five-entities-delete-minimal`** (**2026-04-24**) — **`DELETE`** для пяти **`[id]/route.ts`**: **`204`** / **`400`** / **`404`** / **`409`** (FK: **`lib/is-pg-foreign-key-violation.ts`** — **`23503`**, **`23001`**, сообщения); smoke — одноразовые строки, read-back **GET**, **`409`** на **`DELETE`** sample **`project`** с **`outcome`**; **`README`**, **`MASTER` §6 footnote**; toolchain + live smoke — см. **What Was Completed**

## Done Criteria (фаза 7 — в работе)
- [x] **`P7-app-shell-7-1-minimal`** (**2026-04-24**) — глобальный shell: **`components/layout/app-shell.tsx`** (левая навигация по **`MASTER` §7.1** + верхняя полоса статуса); **`app/layout.tsx`** оборачивает **`AppShell`**; **`app/page.tsx`** — честный placeholder **Today** на **`/`** по **`UI_V1.md`** (без API, форм, auth); toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**51** s **`next build`** на машине агента); ручная проверка: **`npm run dev`** → визуально **nav + top bar** + текст **not_release_ready** / **prototype** (см. **What Was Completed**)
- [x] **`P7-7-2-design-system-minimal`** (**2026-04-24**) — примитивы **`components/v1/`** (`MetaLabel`, `SectionCard`, `StatusBadge` + константы **`WORK_STATUS_BADGES` / `MATURITY_BADGES` / `RISK_BADGES`** по **`MASTER` §7.2**); интеграция в **`AppShell`** и **`/`** (демо-рядок всех токенов); toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**49** s **`next build`**); ручной просмотр **`npm run dev`** (см. **What Was Completed**)
- [x] **`P7-8-1-overview-static-blocks`** (**2026-04-25**) — на **`/`** внутри **`#overview`**: **шесть** статических блоков **`MASTER` §8.1** (`SectionCard` + честный copy из **`STATUS` / `UI_V1` / `UX_V1_EXTRACT` / `RELEASE_CRITERIA_V1`**); **без** fetch; **без** «зелёного театра» в этих блоках (нет L2/L3-claim; **`StatusBadge`** только **`building` / `not_release_ready` / `prototype` / `no_api`** — демо §7.2 внизу страницы отдельно); toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**43** s **`next build`**); см. **What Was Completed**
- [x] **`P7-9-1-outcomes-index-static`** (**2026-04-25**) — **`app/outcomes/page.tsx`**: статический index по **`UI_V1.md`** §3.3 (заголовок, полоса зрелости **`StatusBadge`**, empty-state + явно помеченные **две** статические строки-превью, блок next/blocker); **`AppShell`**: пункт **Outcomes** → **`/outcomes`**, навигация через **`Link`**; **`/`** секция **`#outcomes`**: ссылка на **`/outcomes`**; **без** API из браузера, **без** CRUD/detail; toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (первый прогон **`build`** мог упасть на **Google Fonts** из-за сети; повтор — **exit 0**, ~**30** s **`next build`**); см. **What Was Completed**
- [x] **`P7-9-2-outcomes-index-readonly-api`** (**2026-04-25**) — **`/outcomes`**: серверный **`fetch`** к **`GET /api/projects?slug=fabrika-v1-sample`** → **`GET /api/outcomes?project_id=...`**; **`export const dynamic = 'force-dynamic'`**; **`app/outcomes/loading.tsx`**; состояния **error / empty (нет sample project) / empty (0 outcomes) / ok (список)**; честный copy (read-only, нет CRUD/detail, **`release_readiness`** ≠ L3); **`app/page.tsx`** — уточнение секции **`#outcomes`**; **без** изменений **`app/api/*`**, schema, smoke; toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**41** s **`next build`**); smoke: **`npm run dev -- -p 3055`** → **`curl.exe http://127.0.0.1:3055/outcomes`** → **HTTP 200** (см. **What Was Completed**)
- [ ] **`MASTER` §7–§8** в продуктовом смысле (живой Today + динамика §8.2 + API из UI) — **не** заявлять **done** для фазы 7 до отдельных задач / evidence

## Done Criteria (фаза 5 — закрыта)
- [x] **`MASTER` §5.1** — `AGENTS.md` + перечисленные **`.cursor/rules/*.mdc`** сверены с **`MASTER`**; зафиксирован дополнительный файл **`subagents-delegation.mdc`** (**P5-phase-5-reconcile**, **2026-04-23**)
- [x] **`MASTER` §5.2** — словарь phase/stage/session status; правила **`STATUS` / `NEXT_STEP` / `DECISIONS`**; согласовано с **`AGENTS.md`** + правками **`cursor-executor`**, **`evidence-reviewer`**, **`release-readiness`**, **`subagents-delegation`** (**P5-5.2-protocol**, **2026-04-23**)
- [x] **`MASTER` §5.3** — каталог **`DOCS/templates/`** + четыре файла (**`P5-5.3-templates`**, **2026-04-23**)
- [x] Definition of done §5 в **`MASTER`** — §5.1–§5.3 отражены в репо; шаблоны на месте

## Done Criteria (фаза 3 — закрыта)
- [x] Структура папок из §3.1 (`app/`, `components/`, `lib/`, `db/`, `tests/`, `playwright/`, `DOCS/`, `.cursor/rules/`)
- [x] Корневой `README` с честным статусом и ссылкой на `DOCS/MASTER_TODO_CURSOR.md` (RB-3.1-readme)
- [x] Пункты **§3.2 Initialize stack** — bootstrap выполнен
- [x] §3.3 полностью: скрипты **`db:generate` / `db:migrate`**; **clean bootstrap from zero** — проверено `npm ci` + `npm run typecheck` / `lint` / `test` / `build`; Husky — **намеренно отложено** (**DEC-004**)
- [x] Полный Definition of done §3 + сверка **§3.1–§3.2** в `MASTER_TODO_CURSOR.md` с репозиторием (**RB-3.phase-close-sync**)

## What Was Completed
- **P4-4.3-architecture:** создан **`DOCS/ARCHITECTURE_V1.md`** — цель документа; граница v1; строительные блоки; ссылки на **data / execution / UI/UX** через extracts; **out of scope**; правила для Cursor; «do not assume». В **`MASTER_TODO_CURSOR.md`** отмечена строка **`ARCHITECTURE_V1.md`** в **§4.3**. Обновлены **`DOCS/INDEX.md`** (Primary tier, порядок чтения п. 8, таблица §4, §7 phase navigation), **`STATUS.md`**, **`NEXT_STEP.md`**. **Код / README / DECISIONS** не менялись.
- **P4-4.3-data-model:** создан **`DOCS/DATA_MODEL_V1.md`** — цель; пять сущностей; по каждой: назначение, минимальные поля, связи, что сознательно не моделируется; docs-only vs БД; порядок миграций + индексы; явный out-of-scope; «do not assume»; приоритет узкой трактовки vs **`V1_SCOPE`**. Обновлены **`ARCHITECTURE_V1.md`** (§2 Data, §4, §8 rule 3, §9, document control), **`MASTER_TODO_CURSOR.md`** §4.3 строка **`DATA_MODEL_V1`**, **`INDEX.md`** (Primary, чтение п. 9–14, таблица §4, §7, описание **`ERD - Data Model`**). **`README` / код** не менялись; **`DECISIONS.md`** — без новых записей (рекомендации uuid/bigserial и FK — в тексте **`DATA_MODEL_V1`**, не как DEC).
- **P4-4.3-events:** создан **`DOCS/EVENTS_V1.md`** — цель; что такое state/event в v1; минимальные переходы; docs-only конвенции; явный out-of-scope (брокер, outbox, оркестратор); как пользоваться документом в Cursor; «do not assume»; приоритет **`V1_SCOPE`** при натяжке с extract. Обновлены **`ARCHITECTURE_V1.md`** (§5, §8 п.5, §9, document control), **`MASTER_TODO_CURSOR.md`** §4.3 строка **`EVENTS_V1`**, **`INDEX.md`** (Primary, чтение п. 9–15, таблица §4, §7, описание **`EVENTS_V1_EXTRACT`**). **`README` / код / DECISIONS`** не менялись.
- **P4-4.3-ui:** создан **`DOCS/UI_V1.md`** — цель; канонические поверхности (Today, Outcome, опционально index); по каждой: purpose / primary action / visible elements / key states; связь с solo-founder workflow через **`UX_V1_EXTRACT`** как контекст (не второй UI-спек); out-of-scope UI; «build within / do not assume»; правило узкой трактовки при натяжении с UX. Обновлены **`ARCHITECTURE_V1.md`** (§5–§6, §8 п.4, §9, document control, §5 state bullet), **`MASTER_TODO_CURSOR.md`** §4.3 строка **`UI_V1`**, **`INDEX.md`** (Primary, чтение п. 11–16, таблица §4, §7, **`UI_V1_EXTRACT`** / **`UI.MD`** описания). **`README` / код / DECISIONS`** не менялись.
- **P4-4.3-release-criteria:** создан **`DOCS/RELEASE_CRITERIA_V1.md`** — цель; уровни L1–L3; условия; evidence; human approval; no-go; known limitations; «do not assume»; сжатие духа RFC FSM «guards» без enterprise-политики. Обновлены **`MASTER_TODO_CURSOR.md`** §4.3, **`ARCHITECTURE_V1.md`** (§8 п.4, §9, document control), **`INDEX.md`** (Primary, чтение п. 12–17, таблица §4, §7, **`RFC.md`** описание), **`UI_V1.md`** (ссылка на критерии + document control). **Фаза 4** закрыта в **`STATUS`**; **`NEXT_STEP`** → сверка **§5**. **`README` / код / DECISIONS`** не менялись.
- **P5-phase-5-reconcile:** сверка **`MASTER` §5.1** с репозиторием — подтверждены **`AGENTS.md`**, **`outcome-planner.mdc`**, **`cursor-executor.mdc`**, **`evidence-reviewer.mdc`**, **`release-readiness.mdc`**; зафиксирован дополнительный **`subagents-delegation.mdc`**. **`MASTER_TODO_CURSOR.md` §5.1** обновлён пометками *present* + строка про **`subagents-delegation`**. **`STATUS.md`** (критерий §5.1), **`NEXT_STEP.md`** → **`P5-5.2-protocol`** (только §5.2, **без** §5.3). **`README` / код / остальные product-docs / DECISIONS`** не менялись.
- **P5-5.2-protocol:** расширен **`MASTER` §5.2** (phase / stage / session status + когда обновлять **`STATUS`**, **`NEXT_STEP`**, **`DECISIONS`**, evidence canon); **`AGENTS.md`** — пункт 4 DOCS-first, секция §5.2, Prove/Ship; выровнены четыре статуса в **`cursor-executor.mdc`**, **`evidence-reviewer.mdc`**, **`release-readiness.mdc`**; уточнён **`subagents-delegation.mdc`**. **`README` / код / `DOCS/templates` / unrelated DOCS / DECISIONS`** не менялись.
- **P5-5.3-templates:** созданы **`DOCS/templates/OUTCOME_BRIEF.md`**, **`EVIDENCE_CHECKLIST.md`**, **`RELEASE_REVIEW.md`**, **`SESSION_SUMMARY.md`** (§5.2 / **`RELEASE_CRITERIA_V1`** / deliverable pack); обновлены **`MASTER` §5.3**, **`INDEX.md`** §4, **`STATUS`**, **`NEXT_STEP`**. **`README` / код / DECISIONS / unrelated DOCS`** не менялись.
- **P6-read-master-from-section-6:** сверка **`MASTER` §6–§7** (заголовки) с **`DATA_MODEL_V1.md`** и `db/schema.ts`. **Зазор:** §6.1–6.2 перечисляет **10** устаревших сущностей/имён (вкл. `cursor_runs`, `evidence_bundles`, …); канон v1 — **5** таблиц; в репо только **`_fabrika_bootstrap_probe`**, **ноль** таблиц канона. В **`MASTER` §6** добавлен блок **Canonical v1 alignment**; Definition of done §6 уточнён под пять сущностей. **`STATUS`**, **`NEXT_STEP`** → **`P6-drizzle-v1-schema-migration`**. **Код / README / templates / AGENTS / .mdc / DECISIONS** не менялись. *(Исторический снимок «ноль таблиц» — до последующих миграций; см. секцию **Phase 6 — MASTER §6 vs baseline**.)*
- **P6-master-section6-dod-reconcile (2026-04-24):** сверка **`MASTER` §6** с **`app/api/**/route.ts`** (**только** `GET`/`POST` на пяти маршрутах), **`db/schema.ts`**, **`scripts/seed-sample-project.mjs`**, smoke **`P6-evidence-item-post-read-smoke`**. Результат — таблица **«Phase 6 — MASTER §6 vs baseline»** в этом файле; **`MASTER` / `DECISIONS` / код / README** не менялись.
- **MASTER-§6-dod-footnote-C+R-only (2026-04-24):** **`DOCS/MASTER_TODO_CURSOR.md` §6** — сразу под буллетами Definition of done добавлен blockquote **Footnote — verified baseline vs full CRUD**: verified сегодня = **C+R** для пяти сущностей **`DATA_MODEL_V1`**; **full CRUD** требует **U/D** или явный **`DECISIONS`** о сужении DoD фазы 6 до **C+R-only**; **`in_progress`** до одного из этих условий; ссылки на **`DOCS/STATUS.md`** (таблица Phase 6), **`README.md`** (smoke), **`DECISIONS.md`**. **`README` / код / `DECISIONS` / `AGENTS` / `.mdc`** на этом шаге **не** менялись.
- **README-local-smoke-five-apis (2026-04-24):** **`README.md`** — секция **«Five-entity API smoke (phase-6 C+R baseline)»**: prerequisites (**`DATABASE_URL`** в **`.env.local`**, миграции, **`npm run dev`**), команда **`npm run smoke:api-five`**, опциональный base URL через **`npm run … -- http://127.0.0.1:<port>`**, что проверяет / чего **не** проверяет (U/D, UI, release readiness); в блок **Other useful commands** добавлена одна строка **`npm run smoke:api-five`**. **`AGENTS.md`**, **`.mdc`**, **`app/api`**, **`db/schema`**, **`DECISIONS`** не менялись.
- **P6-outcome-patch-minimal (2026-04-24):** **`app/api/outcomes/[id]/route.ts`** — **`PATCH`**: тело **строго** **`{ "title": "<non-empty>" }`**; **`id`** не UUID → **`400`**; нет строки → **`404` `outcome not found`**; успех → **`200`** **`{ outcome: … }`** (snake_case как **`GET`**), **`updated_at`** обновляется через Drizzle **`set({ title, updatedAt: new Date() })`**. **`app/api/outcomes/route.ts`** — экспорт **`UUID_RE`**, **`mapOutcomeRow`**. Toolchain (**2026-04-24**): **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (**`next build`** ~3.5 min). Smoke: **`npm run dev -- -p 3010`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:3010`** — **exit 0**; шаги **PATCH**: невалидный path **`/api/outcomes/not-a-uuid`** → **`400`**; неизвестный UUID **`00000000-0000-4000-8000-000000000099`** → **`404`**; пустой **`title`** на созданном **`outcome`** → **`400`**; валидный **`PATCH`** → **`200`**, **`GET /api/outcomes?project_id=<sample>`** подтверждает **`smoke-outcome-patched-<run_id>`** для **`outcome.id=cf75226c-7a00-4aa7-81de-939c8165d839`**, **`run_id=1777063493743`**. **`scripts/smoke-api-five-entities.mjs`** — блок проверок **PATCH** после **POST outcome**. **`README.md`** — секция **Five-entity API smoke** обновлена (есть **PATCH outcome**; «нет update» исправлено). **`DECISIONS` / `MASTER` / `AGENTS` / `.mdc`** не менялись.
- **P6-project-patch-minimal (2026-04-24):** **`app/api/projects/[id]/route.ts`** — **`PATCH`**: тело **строго** **`{ "name": "<non-empty>" }`**; **`id`** не UUID → **`400`**; нет строки → **`404` `project not found`**; успех → **`200`** **`{ project: … }`**; **`set({ name, updatedAt: new Date() })`** (**`slug` / `status`** не трогаются). **`app/api/projects/route.ts`** — экспорт **`UUID_RE`**, **`mapProjectRow`**. Toolchain: **`typecheck` / `lint` / `test` / `build`** — **exit 0** (**`next build`** ~39 s). Live smoke: **`npm run dev -- -p 3999`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:3999`** — **exit 0**; **PATCH project**: **`/api/projects/not-a-uuid`** → **`400`**; **`00000000-0000-4000-8000-000000000077`** → **`404`**; пустой **`name`** → **`400`**; валидный **`PATCH`** → **`200`**, read-back **`GET /api/projects?slug=fabrika-smoke-<run_id>`** → имя **`Smoke project patched <run_id>`** для **`project.id=7bd1f1cd-33ec-45bc-8600-9c9a5e50777e`**, **`run_id=1777063874179`**. **`scripts/smoke-api-five-entities.mjs`** — блок **PATCH project** после **POST project**. **`README.md`** — секция smoke: **PATCH project** + **PATCH outcome**. **`DECISIONS` / `MASTER` / `AGENTS` / `.mdc`** не менялись.
- **P6-assumption-patch-minimal (2026-04-24):** **`app/api/assumptions/[id]/route.ts`** — **`PATCH`**: тело **строго** **`{ "description": "<non-empty>" }`**; **`id`** не UUID → **`400`**; нет строки → **`404` `assumption not found`**; успех → **`200`** **`{ assumption: … }`**; **`set({ description, updatedAt: new Date() })`** (**`code`**, **`criticality`**, **`status`**, **`resolution_note`** не трогаются). **`app/api/assumptions/route.ts`** — экспорт **`UUID_RE`**, **`mapAssumptionRow`**. Toolchain (**2026-04-24**): **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (**`next build`** ~32 s). Live smoke: **`npm run dev -- -p 4005`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:4005`** — **exit 0**; **PATCH assumption**: **`/api/assumptions/not-a-uuid`** → **`400`**; **`00000000-0000-4000-8000-000000000066`** → **`404`**; пустой **`description`** → **`400`**; валидный **`PATCH`** → **`200`**, read-back **`GET /api/assumptions?outcome_id=<sample>`** → **`smoke-assumption-patched-<run_id>`** для **`assumption.id=5f69f40a-60ae-435b-aa0c-d22f6135b1d7`**, **`run_id=1777064222203`**. **`scripts/smoke-api-five-entities.mjs`** — блок после **POST assumption**. **`README.md`** — smoke: добавлен **PATCH assumption**. **`DECISIONS` / `MASTER` / `AGENTS` / `.mdc`** не менялись.
- **P6-acceptance-criterion-patch-minimal (2026-04-24):** **`app/api/acceptance-criteria/[id]/route.ts`** — **`PATCH`**: тело **строго** **`{ "description": "<non-empty>" }`**; **`id`** не UUID → **`400`**; нет строки → **`404` `acceptance_criterion not found`**; успех → **`200`** **`{ acceptance_criterion: … }`**; **`set({ description, updatedAt: new Date() })`** (**`code`**, **`required`**, **`sort_order`**, **`status`** не трогаются). **`app/api/acceptance-criteria/route.ts`** — экспорт **`UUID_RE`**, **`mapAcceptanceCriterionRow`**. Toolchain: **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~31 s). Live smoke: **`npm run dev -- -p 4010`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:4010`** — **exit 0**; **PATCH AC**: невалидный id → **`400`**; **`00000000-0000-4000-8000-000000000055`** → **`404`**; пустой **`description`** → **`400`**; валидный **`PATCH`** → **`200`**, read-back **`GET /api/acceptance-criteria?outcome_id=<sample>`** → **`Smoke AC patched <run_id>`** для **`acceptance_criterion.id=8c4525c2-546f-40bb-b9f8-e113af5ea28d`**, **`run_id=1777064546122`**. **`README.md`** — smoke: **PATCH acceptance_criterion**. **`DECISIONS` / `MASTER` / `AGENTS` / `.mdc`** не менялись.
- **P6-evidence-item-patch-minimal (2026-04-24):** **`app/api/evidence-items/[id]/route.ts`** — **`PATCH`**: тело **строго** **`{ "title": "<non-empty>" }`**; **`id`** не UUID → **`400`**; нет строки → **`404` `evidence_item not found`**; успех → **`200`** **`{ evidence_item: … }`**; **`set({ title, updatedAt: new Date() })`** (**`evidence_type`**, **`artifact_ref`**, **`summary`**, **`status`** не трогаются). **`app/api/evidence-items/route.ts`** — экспорт **`UUID_RE`**, **`mapEvidenceRow`**. Toolchain (**повторный прогон при закрытии задачи**): **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (~30 s **`next build`**). Live smoke: **`npm run dev -- -p 4016`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:4016`** — **exit 0**; **PATCH evidence_item**: невалидный id → **`400`**; **`00000000-0000-4000-8000-000000000044`** → **`404`**; пустой **`title`** → **`400`**; валидный **`PATCH`** → **`200`**, read-back **`GET /api/evidence-items?outcome_id=<sample>`** подтверждает **`smoke-evidence-patched-<run_id>`** для **`evidence_item.id=2564ed52-6f78-4470-a130-3f1c9c42c44b`**, **`run_id=1777064866716`**. **`scripts/smoke-api-five-entities.mjs`** — блок **PATCH** после **POST evidence_item**. **`README.md`** — smoke: **PATCH evidence_item** + правка **«does not verify»** (остался только **DELETE**). **`DECISIONS` / `MASTER` / `AGENTS` / `.mdc`** не менялись.
- **P6-five-entities-delete-minimal (2026-04-24):** **`DELETE`** в **`app/api/{projects,outcomes,assumptions,acceptance-criteria,evidence-items}/[id]/route.ts`**: **`204`** при успехе (**`delete`**.**`returning`** → **`404`** если 0 строк); **`400`** невалидный UUID; **`409`** при нарушении FK (**`lib/is-pg-foreign-key-violation.ts`**: **`23503`**, **`23001`** RESTRICT как на локальной БД с русскими сообщениями + эвристика по тексту). **`scripts/smoke-api-five-entities.mjs`**: **`fetchJson`** обрабатывает пустое тело (**`204`**); после **PATCH** — цепочка **DELETE** (порядок: **`evidence_item`** → **`acceptance_criterion`** → **`assumption`** → **`outcome`** → **`project`**) на одноразовых **`id`** + read-back **GET**; затем **`DELETE`** sample **`project.id=a1438677-0879-457e-bb19-363da9986dbb`** → **`409`**, **GET** **`slug=fabrika-v1-sample`** подтверждает, что seed **не** уничтожен. **`tests/is-pg-foreign-key-violation.test.ts`**. Toolchain: **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0**. Live smoke: **`npm run dev -- -p 4020`** → **`node --env-file=.env.local scripts/smoke-api-five-entities.mjs http://127.0.0.1:4020`** — **exit 0**, **`run_id=1777065949717`**, созданные для удаления: **`project.id=558354cf-8e5b-46a4-be19-f0917bf6b764`**, **`outcome.id=cf2ca3b3-b1c3-49d3-a1f1-bca76c2040ee`**, **`assumption.id=a93184a8-b977-4f44-8b9e-8772eb5a12cf`**, **`acceptance_criterion.id=87ea4b70-1d1d-4f1f-8204-f28123bfbf14`**, **`evidence_item.id=7031d483-3a04-4508-8576-580125ee5fab`**. **`README.md`** (секция smoke), **`DOCS/MASTER_TODO_CURSOR.md` §6 footnote**. **`DECISIONS` / `AGENTS` / `.mdc`** не менялись.
- **P7-app-shell-7-1-minimal (2026-04-24):** **`components/layout/app-shell.tsx`** — левая колонка: бренд **Fabrika3** + навигация якорями **`/#overview`**, **`/#outcomes`**, **`/#assumptions`**, **`/#evidence`**, **`/#release-readiness`**, **`/#docs-timeline`** (соответствие **`MASTER` §7.1**); верхняя полоса: статические метки **Phase 7**, **App shell (§7.1)**, **`prototype`**, **`not_release_ready`**. **`app/layout.tsx`** — **`metadata`** Fabrika3, **`AppShell`** вокруг **`children`**, **`font-sans`/`bg-background`**. **`app/page.tsx`** — секция **`#overview`**: честный placeholder **Today** по **`UI_V1.md`** (нет API из UI, нет форм, нет «готово»); ниже — короткие placeholder-блоки для остальных пунктов навигации. Toolchain: **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (~**51** s). **`README` / `DECISIONS` / `AGENTS` / `.mdc` / API / smoke / schema** — **не** менялись.
- **P7-7-2-design-system-minimal (2026-04-24):** **`components/v1/meta-label.tsx`**, **`section-card.tsx`**, **`status-badge.tsx`** — статические пилюли (**`MASTER` §7.2**: work status **8**, maturity **5**, risk **4** + честные **`phase_7`**, **`no_api`**, **`not_release_ready`**); **`app/page.tsx`** — **`SectionCard`** для overview/секций + демо-блок всех токенов; **`app-shell.tsx`** — **`MetaLabel`** в сайдбаре, **`StatusBadge`** в header (**Phase 7**, **prototype**, **not_release_ready**, **no API connected**). Toolchain: **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (~**49** s **`next build`**). **`README` / `DECISIONS` / `AGENTS` / `.mdc` / API / smoke / schema / globals** — **не** менялись (globals не требовались).
- **P7-8-1-overview-static-blocks (2026-04-25):** **`app/page.tsx`** — внутри **`#overview`**: **шесть** вложенных **`SectionCard`** по **`MASTER` §8.1** (current objective, verified summary, blockers, last change, maturity, one next step); copy ссылается на **`STATUS` / `UI_V1` / `UX_V1_EXTRACT` / `RELEASE_CRITERIA_V1`**; **`StatusBadge`**: **`no_api`**, **`building`**, **`not_release_ready`**, **`prototype`** — **без** постановки L2/L3 «продукт verified»; **без** `fetch`. Toolchain: **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**43** s **`next build`**). **`README` / `DECISIONS` / API / schema / smoke** — **не** менялись.
- **P7-9-1-outcomes-index-static (2026-04-25):** **`app/outcomes/page.tsx`** — статический index (**`UI_V1.md`** §3.3): честные дисклеймеры (placeholder, API не из UI, нет CRUD/detail), полоса **`StatusBadge`**, empty-state + две явно подписанные статические строки превью, блок next/blocker; **`components/layout/app-shell.tsx`** — **Outcomes** → **`/outcomes`**, пункты навигации на **`Link`**; **`app/page.tsx`** — секция **`#outcomes`** со ссылкой на **`/outcomes`**. Toolchain: **`typecheck` / `lint` / `test` / `build`** — **exit 0** (**`build`** при первом прогоне возможен фейл **`next/font`** / **Google Fonts** без сети; повтор — **ok**, ~**30** s). **`README` / `DECISIONS` / API / schema / smoke / AGENTS / .mdc`** — **не** менялись.
- **P7-9-2-outcomes-index-readonly-api (2026-04-25):** **`app/outcomes/page.tsx`** — async RSC: внутренний **`fetch`** к **`GET /api/projects?slug=fabrika-v1-sample`** затем **`GET /api/outcomes?project_id=…`** (`headers()` → base URL, **`cache: 'no-store'`**, **`dynamic = 'force-dynamic'`**); ветки **error / empty_seed / ok** + пустой список outcomes; честные дисклеймеры (read-only, нет CRUD/`[id]`, **`release_readiness`** в строке ≠ L3 claim); **`app/outcomes/loading.tsx`**; **`app/page.tsx`** — текст секции **`#outcomes`** синхронизирован. Toolchain: **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**41** s **`next build`**). Smoke: **`npm run dev -- -p 3055`** (с **`.env.local`**) → **`curl.exe -sS http://127.0.0.1:3055/outcomes`** → **HTTP 200**. **`README` / `DECISIONS` / `app/api/*` / schema / seed / smoke / AGENTS / .mdc`** — **не** менялись.
- **P6-scripted-smoke-five-apis (2026-04-24):** добавлены **`scripts/smoke-api-five-entities.mjs`** + npm-скрипт **`smoke:api-five`** (**`package.json`**: `node --env-file=.env.local …`). Прогон: **`npm run dev`** (Next **15.5.15**, порт **3000**) → **`npm run smoke:api-five`** → **exit 0** (~104s). Шаги скрипта: **`spawnSync`** seed с **`--env-file=.env.local`**; затем **`fetch`**: **GET** sample **`project`** по **`slug=fabrika-v1-sample`** → **POST** новый **`project`** (**`slug=fabrika-smoke-<run_id>`**) → **GET** по slug → **GET/POST** **`outcome`** (новый **`title=smoke-outcome-<run_id>`** на sample **`project_id`**) → **GET/POST** **`assumption`** → **GET/POST** **`acceptance_criterion`** (**`code=smoke-ac-<run_id>`**, **`sort_order=99`**) → **GET/POST** **`evidence_item`**. Итоговый JSON (сокращённо): **`run_id`** **`1777061963201`**; **`sample_outcome_id`** **`1de1bfd2-ad8e-47d5-9187-10015dde4579`**; созданные **`id`**: **`project`** **`60dadfff-f1ab-4776-bb4f-46aaff67838c`**, **`outcome`** **`e5853584-8c28-487c-bc43-65a9b8442fb2`**, **`assumption`** **`cb2daf03-a6cd-428f-a9da-22a3074ac756`**, **`acceptance_criterion`** **`6bdea124-6733-46b0-b207-b718e3e40aa2`**, **`evidence_item`** **`5ec1742e-fc8a-471f-91db-ebe5b475eb3b`**. **`README` / `MASTER` / `DECISIONS` / `AGENTS` / `.mdc` / `app/api` логика** на этом шаге **не** менялись (кроме **`package.json`** для npm-скрипта).
- **P6-evidence-item-post-read-smoke (2026-04-24):** **`DATABASE_URL`** присутствует в **`.env.local`** (значение в документацию **не** копируется). **`node --env-file=.env.local scripts/seed-sample-project.mjs`** — **exit 0**; sample **`outcome.id`** = **`1de1bfd2-ad8e-47d5-9187-10015dde4579`**. **`npm run dev -- -p 3021`** (Next **15.5.15** Turbopack, env **`.env.local`**). **`POST http://127.0.0.1:3021/api/evidence-items`** (JSON: **`outcome_id`** выше, **`evidence_type`** **`manual_note`**, **`title`** **`P6-evidence-item-post-read-smoke-2026-04-24`**, **`artifact_ref`** **`local:post-read-smoke`**, **`status`** **`draft`**) → **`201`**, тело содержит **`evidence_item.id`** = **`91b818af-6db4-4ef1-aa00-287118f1b90b`**. **`GET http://127.0.0.1:3021/api/evidence-items?outcome_id=1de1bfd2-ad8e-47d5-9187-10015dde4579`** → **`200`**; в массиве **`evidence_items`** найден элемент с тем же **`id`** (**read-back подтверждён**). Проверка выполнена скриптом **`node -e` `fetch`** из корня репо; dev-сервер остановлен после smoke.
- **P6-evidence-item-post-minimal (2026-04-24):** **`app/api/evidence-items/route.ts`** — добавлен **`POST /api/evidence-items`**: JSON **`outcome_id`** (UUID, существующий **`outcome`**), **`evidence_type`**, **`title`**, **`artifact_ref`**; опционально **`summary`**, **`status`** (иначе **`draft`**); **`400`** на невалидный ввод; **`404`** **`outcome not found`** (предпроверка + **`23503`**); ответ **`201`** **`{ evidence_item: … }`** в том же snake_case, что и элементы **`GET`**. Рефакторинг **`GET`**: общий **`mapEvidenceRow`**. **`README.md`** — пример **`node -e "fetch(...POST...)"`** для smoke. Проверка toolchain (**без** обязательного Postgres на машине CI-агента): **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** — **exit 0** (**2026-04-24**, полный **`next build`** ~6 min на этой машине). **Read-back** через **`GET`** после **`POST`** — **не** автоматизирован в **`npm test`**; честный proof — ручной шаг в **`Current Task`**.

## What Is In Progress
- item: **Фаза 7** — **`P7-9-2-outcomes-index-readonly-api`** закрыт (**verified**). **`Current Task`**: **`P7-9-3-outcomes-index-query-project-id`** (query **`project_id`** на **`/outcomes`**, см. **`NEXT_STEP`**).

## What Is Blocked
- item: нет
  reason:
  severity: low
  unblock_action:

## Risks
- item: Имя каталога репозитория `Fabrika3` vs ограничения npm на `name` в нижнем регистре
  impact: scaffold пришлось собирать во временной папке и переносить файлы
  mitigation: `package.json` → `"name": "fabrika3"`
- item: Нехватка места на диске (ENOSPC)
  impact: невозможно скачать Playwright Chromium; срыв npm при тяжёлых установках
  mitigation: e2e на **системном Edge** (`channel: 'msedge'`); см. **DEC-003**
- item: Vitest 4 + optional native deps (rolldown) на части машин
  impact: падение `vitest run` без binding
  mitigation: пин на **Vitest 2.1.9** + guard в `postcss.config.mjs`
- item: Дублирование **`ERD_V1_EXTRACT`** vs будущий **`DATA_MODEL_V1`**
  impact: расхождения
  mitigation: в **`DATA_MODEL_V1.md`** явно указать роль extract как baseline

## Current Product State
- maturity: prototype
- release_readiness: no
- evidence_state: фаза 3: локальные **`npm ci`**, **`npm run typecheck`**, **`npm run lint`**, **`npm test`**, **`npm run build`** (см. RB-3.3-clean-bootstrap); MASTER §3.1–§3.3 согласован с репозиторием; **2026-04-24** — повторная цепочка **`typecheck` / `lint` / `test` / `build`** после **`P6-evidence-item-post-minimal`** (**exit 0**, см. **What Was Completed**); **2026-04-24** — **E2E smoke Postgres + dev**: seed → **`POST`/`GET /api/evidence-items`** (**`P6-evidence-item-post-read-smoke`**, ids в **What Was Completed**); **2026-04-24** — **`npm run smoke:api-five`** (**`P6-scripted-smoke-five-apis`**, dev **`:3000`**, **exit 0**, см. **What Was Completed**); **2026-04-24** — **`P6-outcome-patch-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** с **PATCH outcome** на **`http://127.0.0.1:3010`** — **exit 0** (см. **What Was Completed**); **2026-04-24** — **`P6-project-patch-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** (**PATCH project** + **PATCH outcome**) на **`http://127.0.0.1:3999`** — **exit 0** (см. **What Was Completed**); **2026-04-24** — **`P6-assumption-patch-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** (**+ PATCH assumption**) на **`http://127.0.0.1:4005`** — **exit 0** (см. **What Was Completed**); **2026-04-24** — **`P6-acceptance-criterion-patch-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** (**+ PATCH acceptance_criterion**) на **`http://127.0.0.1:4010`** — **exit 0** (см. **What Was Completed**); **2026-04-24** — **`P6-evidence-item-patch-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** (**+ PATCH evidence_item**) на **`http://127.0.0.1:4016`** — **exit 0**, **`run_id=1777064866716`**, **`evidence_item.id=2564ed52-6f78-4470-a130-3f1c9c42c44b`** (см. **What Was Completed**); **2026-04-24** — **`P6-five-entities-delete-minimal`**: toolchain **exit 0** + **`smoke-api-five-entities`** (**+ DELETE** пяти сущностей + **`409`** на sample **`project`**) на **`http://127.0.0.1:4020`** — **exit 0**, **`run_id=1777065949717`** (см. **What Was Completed**); **2026-04-24** — **`P7-app-shell-7-1-minimal`**: toolchain **exit 0** (см. **What Was Completed**); UI shell — **ручной** осмотр **`npm run dev`** (браузер): **nav + top bar** + честный текст на **`/`**; **2026-04-24** — **`P7-7-2-design-system-minimal`**: toolchain **exit 0**; UI — **ручной** осмотр **`npm run dev`**: **badges** в header + демо-рядок §7.2 на **`/`**; **2026-04-25** — **`P7-8-1-overview-static-blocks`**: toolchain **`typecheck` / `lint` / `test` / `build`** — **exit 0** (~**43** s **`next build`**); **`/`** — **шесть** блоков **`MASTER` §8.1** (статический copy); **2026-04-25** — **`P7-9-1-outcomes-index-static`**: маршрут **`/outcomes`**, навигация **`AppShell`**, ссылка с **`/`**; toolchain **exit 0** (**`build`** ~**30** s; без сети возможен фейл **`next/font` / Google Fonts** — повторный прогон **ok**); **2026-04-25** — **`P7-9-2-outcomes-index-readonly-api`**: серверный read-only **`/outcomes`** + **`loading.tsx`**; smoke **`dev :3055`** + **`curl.exe /outcomes` → 200**
- db_schema_state: в **`db/schema.ts`** описаны **5 / 5** таблиц **`DATA_MODEL_V1`**; в репозитории есть SQL-миграция **`db/migrations/0000_sparkling_fixer.sql`** (строка «**0 / 5**» в старых заметках — **устарела**, см. **P6-read-master** vs последующий bootstrap). Локальное применение миграций — ответственность среды (**`DATABASE_URL`**).
- active_outcome: v1 governed product-to-code (см. MASTER_TODO §2)

## Files/Artifacts Touched In This Phase
- `DOCS/ARCHITECTURE_V1.md`, `DOCS/DATA_MODEL_V1.md`, `DOCS/EVENTS_V1.md`, `DOCS/UI_V1.md`, `DOCS/RELEASE_CRITERIA_V1.md`, `DOCS/MASTER_TODO_CURSOR.md`, `DOCS/INDEX.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(фаза 4)*  
- `DOCS/MASTER_TODO_CURSOR.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P5-phase-5-reconcile**, фаза 5 §5.1)*  
- `DOCS/MASTER_TODO_CURSOR.md`, `AGENTS.md`, `.cursor/rules/cursor-executor.mdc`, `.cursor/rules/evidence-reviewer.mdc`, `.cursor/rules/release-readiness.mdc`, `.cursor/rules/subagents-delegation.mdc`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P5-5.2-protocol**)*  
- `DOCS/templates/*.md`, `DOCS/MASTER_TODO_CURSOR.md`, `DOCS/INDEX.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P5-5.3-templates**)*  
- `DOCS/MASTER_TODO_CURSOR.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-read-master-from-section-6**)*  
- `app/api/evidence-items/route.ts`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-evidence-item-post-minimal**)*  
- `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-evidence-item-post-read-smoke** — только документирование evidence)*  
- `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-master-section6-dod-reconcile**)*  
- `scripts/smoke-api-five-entities.mjs`, `package.json`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-scripted-smoke-five-apis**)*  
- `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**README-local-smoke-five-apis**)*  
- `DOCS/MASTER_TODO_CURSOR.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**MASTER-§6-dod-footnote-C+R-only**)*  
- `app/api/outcomes/[id]/route.ts`, `app/api/outcomes/route.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-outcome-patch-minimal**)*  
- `app/api/projects/[id]/route.ts`, `app/api/projects/route.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-project-patch-minimal**)*  
- `app/api/assumptions/[id]/route.ts`, `app/api/assumptions/route.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-assumption-patch-minimal**)*  
- `app/api/acceptance-criteria/[id]/route.ts`, `app/api/acceptance-criteria/route.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-acceptance-criterion-patch-minimal**)*  
- `app/api/evidence-items/[id]/route.ts`, `app/api/evidence-items/route.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P6-evidence-item-patch-minimal**)*  
- `app/api/projects/[id]/route.ts`, `app/api/outcomes/[id]/route.ts`, `app/api/assumptions/[id]/route.ts`, `app/api/acceptance-criteria/[id]/route.ts`, `app/api/evidence-items/[id]/route.ts`, `lib/is-pg-foreign-key-violation.ts`, `tests/is-pg-foreign-key-violation.test.ts`, `scripts/smoke-api-five-entities.mjs`, `README.md`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md`, `DOCS/MASTER_TODO_CURSOR.md` *(**P6-five-entities-delete-minimal**)*  
- `components/layout/app-shell.tsx`, `app/layout.tsx`, `app/page.tsx`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P7-app-shell-7-1-minimal**)*  
- `components/v1/meta-label.tsx`, `components/v1/section-card.tsx`, `components/v1/status-badge.tsx`, `components/layout/app-shell.tsx`, `app/page.tsx`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P7-7-2-design-system-minimal**)* 
- `app/page.tsx`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P7-8-1-overview-static-blocks**)* 
- `app/outcomes/page.tsx`, `components/layout/app-shell.tsx`, `app/page.tsx`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P7-9-1-outcomes-index-static**)* 
- `app/outcomes/page.tsx`, `app/outcomes/loading.tsx`, `app/page.tsx`, `DOCS/STATUS.md`, `DOCS/NEXT_STEP.md` *(**P7-9-2-outcomes-index-readonly-api**)* 

## Notes
- **Форматирование (format):** Prettier **намеренно отложен** (см. предыдущие Notes).
- **`npm run test:e2e`** не входил в минимальную цепочку верификации bootstrap; предпосылки — **DEC-003** и README.
- **§5.2:** phase (MASTER) ≠ stage (`AGENTS` routing) ≠ session outcome status (четыре токена) — сведено в **`MASTER` §5.2** + **`AGENTS.md`**.
- **§6+ vs канон (после `P6-read-master-from-section-6`):** **`MASTER` §7+** описывает UI/экраны — **не** стартовать до миграции пяти таблиц и явного **`NEXT_STEP`**, иначе расхождение с **`UI_V1`**. Канон БД = **`DATA_MODEL_V1`**; старый список §6.1 — только с пометкой в **`MASTER` §6**.
- **`MASTER` §6 phrase «CRUD … works»:** blockquote **Footnote — verified baseline vs full CRUD** в **`MASTER` §6** (обновлён под **PATCH+DELETE**); таблица **Phase 6 — MASTER §6 vs baseline** выше. **Update / Delete:** минимальные **`PATCH`** + **`DELETE`** для **всех пяти** — **verified** (**smoke**).
